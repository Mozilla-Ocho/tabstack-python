name: Publish to PyPI

on:
  release:
    types: [created]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set version from release tag
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Releasing version: $VERSION"

          # Update version in pyproject.toml
          sed -i 's/version = ".*"/version = "'$VERSION'"/' pyproject.toml

          # Update version in __init__.py
          sed -i 's/__version__ = ".*"/__version__ = "'$VERSION'"/' tabstack/__init__.py

          # Verify versions were updated
          echo "pyproject.toml version:"
          cat pyproject.toml | grep "^version"
          echo "tabstack/__init__.py version:"
          cat tabstack/__init__.py | grep "^__version__"

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-python-3.12-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            pip-${{ runner.os }}-python-3.12-
            pip-${{ runner.os }}-

      - name: Install build dependencies
        run: |
          python3.12 -m pip install --upgrade pip
          python3.12 -m pip install build twine

      - name: Build package
        run: |
          python3.12 -m build

      - name: Check package
        run: |
          python3.12 -m twine check dist/*

      - name: Publish to TestPyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: |
          python3.12 -m twine upload --repository testpypi dist/*

      - name: Verify TestPyPI installation
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Waiting 30 seconds for TestPyPI to process the package..."
          sleep 30

          echo "Installing from TestPyPI..."
          python3.12 -m pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ tabstack==$VERSION

          echo "Testing import..."
          python3.12 -c "import tabstack; print(f'Successfully imported tabstack version {tabstack.__version__}')"

          echo "✓ TestPyPI package verified successfully!"

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          python3.12 -m twine upload dist/*
